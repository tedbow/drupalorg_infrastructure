#!/bin/bash

#set -uex
###Copy into jenkins, remove first comment and add prod db name
#PRODDB=""
#[ -z "$PRODDB" ] && exit 1
#[ ! -f /etc/dbdump/conf ] && exit 1
#source /etc/dbdump/conf
###ssh to prod db server mysql dump
#ssh ${SSHUSER}@${STAGINGDBSERVER} "${SCRIPTDIR}/snapshot2/snapshot_prod-dump.sh ${PRODDB} ${PRODDUMPDIR}"
